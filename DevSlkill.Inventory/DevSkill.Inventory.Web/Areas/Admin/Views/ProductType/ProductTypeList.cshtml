@{
    ViewData["Title"] = "Barcode Type List";
}

<style>
    .toast {
        background-color: #28a745;
        color: white;
    }

    .toast-body {
        width: 100%;
        text-align: center;
    }

</style>

@section Styles {
    <!-- DataTables -->
    <link rel="stylesheet" href="~/adminlte/plugins/datatables-bs4/css/dataTables.bootstrap4.min.css">
    <link rel="stylesheet" href="~/adminlte/plugins/datatables-responsive/css/responsive.bootstrap4.min.css">
    <link rel="stylesheet" href="~/adminlte/plugins/datatables-buttons/css/buttons.bootstrap4.min.css">
}

<!-- Toast container -->
<div class="toast-container position-fixed bottom-0 end-0 p-3" style="z-index: 1100">
    <div id="productTypeToast" class="toast" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="toast-body d-flex" id="toastMessage">
            <!-- Toast message will be dynamically set here -->
        </div>
    </div>
</div>

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>All Warranty List</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                    <li class="breadcrumb-item active">Product Type</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">All available product type are here!</h3>

                        <a class="btn btn-primary float-right" data-toggle="modal" data-target="#addProductTypeModal">
                            <i class="nav-icon"></i>
                            <i class="fas fa-plus"></i> Add Product Type
                        </a>


                    </div>
                    <!-- /.card-header -->
                    <div class="card-body">
                        <partial name="_ResponsePartial" />
                        <partial name="_ModalPartial" />
                        <table id="productType" class="table table-bordered table-striped">
                            <thead>
                                <tr>
                                    <th>Product Type Name</th>
                                    <th>Description</th>
                                    <th>Product Type Code</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>

                            <tfoot>
                                <tr>
                                    <th>Product Type Name</th>
                                    <th>Description</th>
                                    <th>Product Type Code</th>
                                    <th>Actions</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
</section>

<partial name="_AddProductTypeModal" />
<partial name="_UpdateProductTypeModal" />

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="delete-modal" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalLabel">Delete Confirmation</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete this record?</p>
                <p>If you delete this it gone forever!</p>
            </div>
            <div class="modal-footer">
                <!-- The form that will be submitted when delete is confirmed -->
                <form method="POST" id="productType_form" asp-antiforgery="true" , enctype="multipart/form-data">
                    <!-- Hidden input to store the ID of the item to be deleted -->
                    <input type="hidden" id="delete-id" name="id">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-danger" id="productType-delete">Delete</button>
                </form>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <!-- DataTables  & Plugins -->
    <script src="~/adminlte/plugins/datatables/jquery.dataTables.min.js"></script>
    <script src="~/adminlte/plugins/datatables-bs4/js/dataTables.bootstrap4.min.js"></script>
    <script src="~/adminlte/plugins/datatables-responsive/js/dataTables.responsive.min.js"></script>
    <script src="~/adminlte/plugins/datatables-responsive/js/responsive.bootstrap4.min.js"></script>
    <script src="~/adminlte/plugins/datatables-buttons/js/dataTables.buttons.min.js"></script>
    <script src="~/adminlte/plugins/datatables-buttons/js/buttons.bootstrap4.min.js"></script>
    <script src="~/adminlte/plugins/jszip/jszip.min.js"></script>
    <script src="~/adminlte/plugins/pdfmake/pdfmake.min.js"></script>
    <script src="~/adminlte/plugins/pdfmake/vfs_fonts.js"></script>
    <script src="~/adminlte/plugins/datatables-buttons/js/buttons.html5.min.js"></script>
    <script src="~/adminlte/plugins/datatables-buttons/js/buttons.print.min.js"></script>
    <script src="~/adminlte/plugins/datatables-buttons/js/buttons.colVis.min.js"></script>

    <script type="text/javascript">
        //Load data in table used by DataTable...
        $(function () {
            $("#productType").DataTable({
                processing: true,
                serverSide: true,
                responsive: true,
                lengthChange: true,
                searching: true,
                ordering: true,
                info: true,
                autoWidth: false,
                ajax: {
                    url: "/Admin/ProductType/GetProductTypeJsonData",
                    type: "POST",
                    contentType: "application/json",
                    dataType: "json",
                    data: function (d) {
                        return JSON.stringify(d);
                    }
                },

                columnDefs: [
                    {
                        // Render description column with only 20 words
                        targets: 1,
                        render: function (data, type, row) {
                            var words = data.split(" ");
                            if (words.length > 10) {
                                return words.slice(0, 10).join(" ") + '...';
                            }
                            return data;
                        }
                    },

                    {
                        orderable: false,
                        targets: 3,
                        render: function (data, type, row) {
                            return `<button type="button" class="btn btn-info btn-sm update-productType" data-id='${data}' data-toggle="modal" data-target="#updateProductTypeModal">
                                                                                <i class="fas fa-pencil-alt"></i> Edit
                                                                            </button>

                                                                            <button type="submit" class="btn btn-danger btn-sm delete_productType" data-id='${data}' value='${data}'>
                                                                                <i class="fas fa-trash">
                                                                                </i>
                                                                                Delete
                                                                            </button>`;
                        }
                    },
                ],

                dom: '<"row"<"col-sm-12 col-md-6"lB><"col-sm-12 col-md-6"f>><"row"<"col-sm-12"tr>><"row"<"col-sm-12 col-md-5"i><"col-sm-12 col-md-7"p>>',
                "buttons": ["copy", "csv", "excel", "pdf", "print", "colvis"]

            }).buttons().container().appendTo('#productType_wrapper .col-md-6:eq(0)');


            //This code create a new product Type...
            $(document).ready(function () {
                // Handle form submission
                $('#productTypeForm').on('submit', function (e) {
                    e.preventDefault();

                    $.ajax({
                        url: '/Admin/ProductType/AddProductType',
                        method: 'POST',
                        data: $(this).serialize(),
                        success: function (response) {
                            if (response.success) {
                                // Close the modal
                                $('#addProductTypeModal').modal('hide');
                                $('#productTypeForm')[0].reset();

                                // Display toast notification
                                $('#toastMessage').text(response.message);
                                $('#productTypeToast').toast({
                                    delay: 3000
                                });
                                $('#productTypeToast').toast('show');
                                // Refresh the DataTable
                                $('#productType').DataTable().ajax.reload();
                            }
                            else {
                                // Handle validation errors or any other errors
                                // Display toast notification
                                $('#toastMessage').text(response.message);
                                $('#productTypeToast').toast({
                                    delay: 3000
                                });
                                $('#productTypeToast').toast('show');
                            }
                        },
                        error: function (error) {
                            $('#toastMessage').text('There was an error adding the product type.');
                            $('#productTypeToast').toast({
                                delay: 3000
                            });
                            $('#productTypeToast').toast('show');
                        }
                    });
                });
            });

            
            //This code for product Type Update...
            $(document).ready(function () {
                // Handle the click event for the edit button
                $('#productType').on('click', '.update-productType', function () {
                    var productTypeId = $(this).data('id');
                    console.log("button is clicked");

                    // Use AJAX to fetch the category data
                    $.ajax({
                        url: '/Admin/ProductType/GetProductTypeById',
                        method: 'GET',
                        data: { id: productTypeId },
                        success: function (response) {
                            console.log(response)
                            if (response.success) {
                                // Populate the modal fields with the category data
                                $('#updateProductTypeId').val(response.data.id);
                                $('#editproductTypeName').val(response.data.productTypeName);
                                $('#editProductTypeCode').val(response.data.productTypeCode);
                                $('#editProductescription').val(response.data.description);

                                // Open the modal (bootstrap handles this)
                                $('#updateProductTypeModal').modal('show');
                            }
                            else {
                                alert('Error fetching category data');
                            }
                        },
                        error: function () {
                            alert('An error occurred while fetching the category data.');
                        }
                    });
                });

                // Handle the form submission for editing a product type
                $('#updateProductTypeForm').on('submit', function (e) {
                    e.preventDefault();

                    $.ajax({
                        url: '/Admin/ProductType/UpdateProductType',
                        method: 'POST',
                        data: $(this).serialize(),
                        success: function (response) {
                            console.log(response);
                            if (response.success) {
                                // Close the modal and reset
                                $('#updateProductTypeModal').modal('hide');
                                $('#updateProductTypeForm')[0].reset();

                                // Display toast notification
                                $('#toastMessage').text(response.message);
                                $('#productTypeToast').toast({
                                    delay: 3000
                                });
                                $('#productTypeToast').toast('show');

                                // Refresh the DataTable
                                $('#productType').DataTable().ajax.reload();
                            }
                            else {
                                $('#toastMessage').text(response.message);
                                $('#productTypeToast').toast({
                                    delay: 3000
                                });
                                $('#productTypeToast').toast('show');
                            }
                        },
                        error: function () {
                            $('#toastMessage').text('There was an error editing the brand.');
                            $('#productTypeToast').toast({
                                delay: 3000
                            });
                            $('#productTypeToast').toast('show');
                        }
                    });
                });
            });

     
            //This is code delete the product type...
            $(document).ready(function () {
                // Handle the click event on delete buttons in the list
                $('#productType').on('click', '.delete_productType', function (e) {
                    let id = $(this).data("id");  // Get the ID of the item to delete
                    $("#delete-id").val(id);  // Set the hidden input value with the ID
                    $("#delete-modal").modal('show');  // Show the delete confirmation modal
                });

                // Handle the delete confirmation button click
                $("#productType-delete").click(function () {
                    let id = $("#delete-id").val();  // Get the ID from the hidden input
                    // Send the delete request via AJAX
                    $.ajax({
                        url: '/Admin/ProductType/DeleteProductType',
                        type: 'POST',
                        data: {
                            id: id,
                            __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val()  // Include anti-forgery token
                        },
                        success: function (response) {
                            if (response.success) {
                                // Close the modal
                                $('#delete-modal').modal('hide');

                                // Display toast notification
                                $('#toastMessage').text(response.message);
                                $('#productTypeToast').toast({
                                    delay: 3000
                                });
                                $('#productTypeToast').toast('show');
                                // Refresh the DataTable
                                $('#productType').DataTable().ajax.reload();
                            }
                            else {
                                $('#toastMessage').text(response.message);
                                $('#productTypeToast').toast({
                                    delay: 3000
                                });
                                $('#productTypeToast').toast('show');
                            }
                        },
                        error: function (error) {
                            $('#toastMessage').text('There was an error adding the brand.');
                            $('#productTypeToast').toast({
                                delay: 3000
                            });
                            $('#productTypeToast').toast('show');
                        }
                    });
                });
            }); 

            //This code for temp data response fadeOut
            $(document).ready(function () {
                setTimeout(function () {
                    $('#response-alert').fadeOut('slow');
                }, 3000);
            });

        });
    </script>
}