@model IEnumerable<DevSkill.Inventory.Web.Areas.Admin.Models.UserClaimsViewModel>

<style>
    .custom-table-bordered th,
    .custom-table-bordered td {
        border: 1px solid #dee2e6 !important;
    }
</style>

<div class="container-fluid px-5 py-3">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">User Claims</h2>
        <partial name="_ResponsePartial" />
        <a asp-area="Admin" asp-controller="Member" asp-action="AddClaim" class="btn btn-success">Add New Claim</a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-bordered custom-table-bordered mb-0">
            <thead class="table-light">
                <tr>
                    <th class="align-middle">User Name</th>
                    <th class="align-middle">Email</th>
                    <th class="align-middle">Claim Type</th>
                    <th class="align-middle">Claim Value</th>
                    <th class="align-middle">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var user in Model)
                {
                    var claimCount = user.Claims.Count;
                    if (claimCount == 0)
                    {
                        <tr>
                            <td>@user.UserName</td>
                            <td>@user.Email</td>
                            <td colspan="3" class="text-center">No claims available</td>
                        </tr>
                    }
                    else
                    {
                        for (int i = 0; i < claimCount; i++)
                        {
                            <tr>
                                @if (i == 0)
                                {
                                    <td rowspan="@claimCount">@user.UserName</td>
                                    <td rowspan="@claimCount">@user.Email</td>
                                }
                                <td>@user.Claims[i].Type</td>
                                <td>@user.Claims[i].Value</td>
                                <td>
                                    <a asp-area="Admin" asp-action="EditClaim" asp-route-userId="@user.UserId" asp-route-claimType="@user.Claims[i].Type" class="btn btn-primary btn-sm">Edit</a>
                                    <a asp-area="Admin" asp-action="DeleteClaim" asp-route-userId="@user.UserId" asp-route-claimType="@user.Claims[i].Type" asp-route-claimValue="@user.Claims[i].Value"
                                       class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this claim?');">Delete</a>
                                </td>

                            </tr>
                        }
                    }
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        // Code for temp data response fadeOut
        $(document).ready(function () {
            setTimeout(function () {
                $('#response-alert').fadeOut('slow');
            }, 3000);
        });
    </script>
}
